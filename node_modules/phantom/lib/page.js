'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/**
 * Page class that proxies everything to phantomjs
 */

var Page = function Page(phantom, pageId) {
    _classCallCheck(this, Page);

    this.target = 'page$' + pageId;
    this.phantom = phantom;
};

exports.default = Page;


var methods = ['open', 'render', 'close', 'property', 'injectJs', 'includeJs', 'openUrl', 'stop', 'renderBase64', 'evaluate', 'evaluateJavaScript', 'setting', 'addCookie', 'deleteCookie', 'clearCookies', 'setContent', 'sendEvent'];

methods.forEach(function (method) {
    Page.prototype[method] = function () {
        return this.phantom.execute(this.target, method, [].slice.call(arguments));
    };
});